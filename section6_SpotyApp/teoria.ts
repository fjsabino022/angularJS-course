/**
 * En angular, en los archivos .HTML, podemos tener por ejemplo la siguiente sentencia:
 * 
 * <input [(ngModel)]="termino" type="text" 
      name="termino" placeholder="Buscar por artista...">
 *  Los parentesis en angular sirven para hacer llamados de eventos.
 *  Las llaves sirven para establecer que vamos a recibir valores.
 *  Esta anotacion nos ayuda a emitir y escuchar desde en este caso un input.
 * 
 */

